//首行 ,指定版本语法
syntax="proto3";
//注意,同级的枚举类型,不能再去定义一个相同名的常量
// 且导入文件的枚举文件,同级也不能有想相同量的常量---解决:命名package
enum PhoneType{
    MP = 0;//0值常量必须在,且在开头,在32位以内,且不推荐负值,同时,也可以嵌套定义  移动电话
    TEL =1;//                                                           固定电话        
}
// message Phone{
//     enum PhoneType{
//     MP = 0;//0值常量必须在,且在开头,在32位以内,且不推荐负值,同时,也可以嵌套定义
//     TEL =1;
// }
// }